from django.contrib import admin

# Remove model registrations as they are no longer Django models
# admin.site.unregister(User)
# admin.site.unregister(Team)
# admin.site.unregister(Activity)
# admin.site.unregister(Leaderboard)
# admin.site.unregister(Workout)
